flask==3.0.0
flask-cors==4.0.0
numpy==1.24.3
pillow==10.2.0
gunicorn==21.2.0

# PyTorch dependencies (for CORAL model)
# Note: torch and torchvision installed separately in Dockerfile

# Optional: ONNX runtime if using ONNX model
# onnxruntime==1.15.1
